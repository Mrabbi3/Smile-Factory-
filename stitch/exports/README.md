# Stitch Exports

These files are containerized versions of the original Stitch MCP exports.

## Layout

- get-project/: project metadata, theme assets, and screen instance data
- screens/: one file per exported reference screen plus an index
- tools/: one file per Stitch MCP tool schema plus an index

The root stitch-*.json files in the workspace are lightweight indexes that point here.
